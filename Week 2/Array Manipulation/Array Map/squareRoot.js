/**
 * Using the squareRoot function, map each element in the array arr to its square root. 
 * Return the new array.
 */

function squareRoot(arr) {
    let a = arr.map((ele) =>
        ele = Math.sqrt(ele)
    )
    return a
}

module.exports = squareRoot;
