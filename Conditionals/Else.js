/*
Let's complete the isNotEqual function!
If a is not equal to b return true.
*/

function isNotEqual(a, b) {
    if(a!==b)
    return true;
    else
    return false;
}

module.exports = isNotEqual;